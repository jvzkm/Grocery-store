CREATE TABLE t_worker (
  id INT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   name VARCHAR(255) NOT NULL,
   surname VARCHAR(255) NOT NULL,
   salary DOUBLE PRECISION NOT NULL,
   job_id INT NOT NULL,
   store_id INT NOT NULL,
   CONSTRAINT pk_t_worker PRIMARY KEY (id)
);

ALTER TABLE t_worker ADD CONSTRAINT FK_T_WORKER_ON_JOB FOREIGN KEY (job_id) REFERENCES t_job (id);

ALTER TABLE t_worker ADD CONSTRAINT FK_T_WORKER_ON_STORE FOREIGN KEY (store_id) REFERENCES t_store (id);
